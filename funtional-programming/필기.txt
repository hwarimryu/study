
* 외부 다형성

    메서드-> 객체에 종속되어있음
        -> 해당 클래스의 객체에서만 사용할 수 있음.
        -> 객체지향의 특징

    함수-> 객체와 별개 -> 다형성이 굉장히 높다.

* 내부 다형성은 보조함수가 책임진다.

    ex) predi, iter, mapper 는 보조 함수

* 커링

    함수에 인자를 하나씩 적용하다가 필요한 인자가 채워지면 함수 전체가 실행되는 기법
    ex) _curry, _curryr